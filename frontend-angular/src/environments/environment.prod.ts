// Environment configuration for production
export const environment = {
    production: true,
    apiUrl: '/api', // Relative URL for production (adjust based on your deployment)
    appName: 'HomeBites',
    version: '1.0.0'
};
